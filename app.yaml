application: yahtzee-142109
version: 1
runtime: python27
api_version: 1
threadsafe: yes

# goes without saying but change this in prod
env_variables:
  JWT_KEY: "The secret, secret key"

handlers:
- url: /favicon\.ico
  static_files: favicon.ico
  upload: favicon\.ico

- url: .*
  script: main.app

libraries:
- name: webapp2
  version: "2.5.2"
